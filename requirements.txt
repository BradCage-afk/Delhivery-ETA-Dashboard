# Slim runtime deps for the Streamlit dashboard ONLY (no training/torch needed).
# Use this file when deploying dashboard/app.py to Streamlit Cloud / HF Spaces / etc.
streamlit>=1.40,<2
pandas==2.3.3,<3
numpy==2.4.3,<3
lightgbm==4.6
joblib==1.3
pyarrow>=22
altair>=5.0
pydeck>=0.9
